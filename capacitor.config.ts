import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'stories-for-kids',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
